query GetAllOrders {
  orders {
    id
    name
  }
}

mutation CreateOrder($order: CreateOrderInput!) {
  createOrder(order: $order) {
    id,
    name
  }
}

subscription OnNotification {
  notificationAdded {
    text
    createdAt
  }
}
