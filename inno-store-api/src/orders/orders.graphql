type Query {
  orders: [Order!]!
  order(id: ID!): Order
}

type Mutation {
  createOrder(order: CreateOrderInput): Order
  deleteOrder(orderId: ID!): Order
}

type Order {
  id: String!
  name: String!
}

input CreateOrderInput {
  id: String!
  name: String!
}
